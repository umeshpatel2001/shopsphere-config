server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/productdb
    username: myuser
    password: mypassword

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/